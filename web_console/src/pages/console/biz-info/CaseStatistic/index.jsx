import { useEffect } from "react";

const CaseStatistic = () => {
    useEffect(() => {
        console.log('CaseStatistic page mounted.');
    },[]);

    return (
        <div>
            CaseStatistic.
        </div>
    );
}

export default CaseStatistic;